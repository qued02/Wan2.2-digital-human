# Wan2.2-Animate-14B 模型运行完整流程说明

## 📋 目录
1. [整体架构](#整体架构)
2. [完整运行流程](#完整运行流程)
3. [各阶段详细说明](#各阶段详细说明)
4. [关键代码位置](#关键代码位置)

---

## 🏗️ 整体架构

```
用户输入
  ↓
[前端界面/Gradio] → 参数配置
  ↓
[预处理阶段] preprocess_data.py
  ├─ 视频处理
  ├─ 图像处理
  ├─ 姿态提取
  └─ 数据准备
  ↓
[生成阶段] generate.py → wan/animate.py
  ├─ 模型加载
  ├─ 文本编码 (T5)
  ├─ 图像编码 (CLIP)
  ├─ VAE编码
  ├─ 扩散采样 (DiT)
  └─ VAE解码
  ↓
[后处理] 保存视频
```

---

## 🔄 完整运行流程

### **阶段一：预处理 (Preprocessing)**

**文件位置**: `wan/modules/animate/preprocess/preprocess_data.py`

**主要步骤**:

1. **参数解析与验证**
   ```python
   # 解析命令行参数
   - ckpt_path: 预处理模型检查点路径
   - video_path: 驱动视频路径
   - refer_path: 参考图片路径
   - save_path: 预处理结果保存路径
   - resolution_area: 目标分辨率 [width, height]
   - replace_flag: 是否使用替换模式
   - retarget_flag: 是否使用姿态重定向
   ```

2. **视频预处理**
   - 读取驱动视频
   - 调整视频分辨率（保持宽高比，总面积 = width × height）
   - 提取视频帧序列
   - 统一帧率（默认30fps）

3. **图像预处理**
   - 读取参考图片
   - 调整图片尺寸匹配视频分辨率
   - 图像归一化

4. **姿态提取与处理**
   - 使用姿态检测模型提取人体关键点
   - 生成姿态序列（每帧对应一个姿态）
   - 如果启用 `retarget_flag`，进行姿态重定向
   - 如果启用 `replace_flag`，生成替换掩码

5. **数据保存**
   - 保存处理后的视频帧
   - 保存参考图片
   - 保存姿态数据
   - 保存掩码数据（如果使用替换模式）
   - 生成配置文件

**输出目录结构**:
```
process_results/
├── video/          # 处理后的视频帧
├── reference/      # 参考图片
├── pose/          # 姿态数据
├── mask/          # 掩码数据（替换模式）
└── config.json    # 配置文件
```

---

### **阶段二：模型初始化 (Model Initialization)**

**文件位置**: `generate.py` → `wan/animate.py`

**主要步骤**:

1. **参数解析** (`generate.py` 第105-300行)
   ```python
   # 关键参数
   --task: 任务类型 (animate-14B)
   --ckpt_dir: 模型权重目录
   --src_root_path: 预处理数据路径
   --size: 输出分辨率 (如 1280*720)
   --sample_steps: 采样步数
   --sample_guide_scale: 引导系数
   --refert_num: 参考帧数 (1或5)
   --replace_flag: 是否使用替换模式
   ```

2. **分布式初始化** (`generate.py` 第315-343行)
   ```python
   # 单GPU模式
   - rank = 0, world_size = 1
   - 不使用分布式训练
   
   # 多GPU模式
   - 初始化NCCL后端
   - 设置进程组
   - 分配GPU设备
   ```

3. **模型组件加载** (`wan/animate.py` 第36-143行)
   ```python
   # 加载三个主要组件：
   
   # 1. T5文本编码器
   self.text_encoder = T5EncoderModel(...)
   - 功能: 将文本提示词编码为特征向量
   - 位置: CPU或GPU（根据t5_cpu参数）
   - 模型: umt5-xxl
   
   # 2. CLIP图像编码器
   self.clip = CLIPModel(...)
   - 功能: 编码参考图片和视频帧
   - 位置: GPU
   - 模型: open-clip-xlm-roberta-large-vit-huge-14
   
   # 3. VAE编码解码器
   self.vae = Wan2_1_VAE(...)
   - 功能: 图像/视频的编码（压缩）和解码（重建）
   - 位置: GPU
   - 模型: Wan2.1_VAE
   
   # 4. DiT扩散模型（主模型）
   self.noise_model = WanAnimateModel.from_pretrained(...)
   - 功能: 在潜在空间中进行扩散采样
   - 位置: GPU
   - 模型: Wan2.2-Animate-14B
   ```

4. **模型配置** (`wan/animate.py` 第144-200行)
   - 设置评估模式
   - 应用分布式策略（FSDP/序列并行）
   - 加载LoRA权重（如果使用替换模式）
   - 精度转换（bfloat16/fp16）

---

### **阶段三：数据准备 (Data Preparation)**

**文件位置**: `wan/animate.py` 的 `generate()` 方法

**主要步骤**:

1. **加载预处理数据**
   ```python
   # 从 src_root_path 加载：
   - 视频帧序列
   - 参考图片
   - 姿态数据
   - 掩码数据（如果使用替换模式）
   ```

2. **文本编码** (`wan/animate.py` 第369-376行)
   ```python
   # 使用T5编码器
   context = self.text_encoder([prompt], device)
   context_null = self.text_encoder([negative_prompt], device)
   
   # 输出: 文本特征向量 [batch, seq_len, hidden_dim]
   ```

3. **图像编码** (`wan/animate.py` 第508-513行)
   ```python
   # 使用CLIP编码器
   # 编码参考图片
   ref_latents = self.vae.encode(ref_pixel_values)
   
   # 编码视频帧（用于条件引导）
   pose_latents = self.vae.encode(conditioning_pixel_values)
   ```

4. **VAE编码** (`wan/animate.py` 第508-568行)
   ```python
   # 将图像从像素空间编码到潜在空间
   # 参考帧编码
   ref_latents = self.vae.encode(ref_pixel_values)
   
   # 时间引导帧编码（refert_num帧）
   y_reft = self.vae.encode(reference_frames)
   
   # 输出: 潜在空间特征 [batch, channels, frames, height, width]
   ```

---

### **阶段四：扩散采样 (Diffusion Sampling)**

**文件位置**: `wan/animate.py` 的 `generate()` 方法

**主要步骤**:

1. **初始化噪声** (`wan/animate.py` 第570-580行)
   ```python
   # 在潜在空间中生成随机噪声
   x = torch.randn(...)  # 形状匹配目标视频
   ```

2. **采样循环** (`wan/animate.py` 第580-630行)
   ```python
   for t in timesteps:  # 从噪声到清晰，逐步去噪
       # 1. 模型前向传播
       noise_pred = self.noise_model(
           x,                    # 当前噪声状态
           timestep=t,           # 当前时间步
           context=context,      # 文本特征
           ref_latents=ref_latents,  # 参考帧特征
           pose_latents=pose_latents,  # 姿态特征
           ...
       )
       
       # 2. 采样器更新（UniPC或DPM++）
       x = scheduler.step(noise_pred, t, x)
       
       # 3. 模型卸载（如果启用offload_model）
       if offload_model:
           self.noise_model.cpu()
   ```

3. **采样器类型**
   - **UniPC**: 快速采样，推荐用于动画任务
   - **DPM++**: 高质量采样，速度较慢

4. **时间引导** (`wan/animate.py` 第520-568行)
   ```python
   # refert_num 控制使用多少帧作为时间引导
   # refert_num=1: 只使用第一帧
   # refert_num=5: 使用前5帧
   # 更多参考帧 = 更好的时间一致性，但计算量更大
   ```

---

### **阶段五：VAE解码与后处理**

**文件位置**: `wan/animate.py` 第637行

**主要步骤**:

1. **VAE解码** (`wan/animate.py` 第637行)
   ```python
   # 将潜在空间特征解码回像素空间
   out_frames = self.vae.decode([x0])
   
   # 输出: 视频帧张量 [batch, frames, height, width, channels]
   ```

2. **后处理**
   - 张量归一化（从[-1,1]转换到[0,1]）
   - 转换为numpy数组
   - 转换为uint8格式（0-255）

3. **保存视频** (`generate.py` 第554-561行)
   ```python
   save_video(
       tensor=video[None],
       save_file=args.save_file,
       fps=cfg.sample_fps,  # 默认30fps
       normalize=True,
       value_range=(-1, 1)
   )
   ```

---

## 📝 各阶段详细说明

### **1. 预处理阶段详细流程**

**文件**: `wan/modules/animate/preprocess/preprocess_data.py`

```python
# 主要处理流程
ProcessPipeline(
    ckpt_path=args.ckpt_path,      # 预处理模型路径
    video_path=args.video_path,    # 驱动视频
    refer_path=args.refer_path,    # 参考图片
    save_path=args.save_path,      # 保存路径
    resolution_area=args.resolution_area,  # 分辨率
    fps=args.fps,                  # 帧率
    replace_flag=args.replace_flag, # 替换模式
    retarget_flag=args.retarget_flag,  # 姿态重定向
    ...
).run()
```

**处理内容**:
- 视频帧提取与调整
- 人体姿态检测（使用DW-Pose）
- 姿态序列生成
- 掩码生成（替换模式）
- 数据格式转换

---

### **2. 生成阶段详细流程**

**文件**: `wan/animate.py` 的 `generate()` 方法

#### **2.1 数据加载** (第470-510行)

```python
# 加载预处理数据
src_root_path = args.src_root_path
- 读取视频帧
- 读取参考图片
- 读取姿态数据
- 读取掩码数据（如果使用替换模式）
```

#### **2.2 条件编码** (第508-568行)

```python
# 文本条件
context = self.text_encoder([prompt])  # T5编码

# 图像条件
ref_latents = self.vae.encode(ref_image)  # 参考图片编码

# 姿态条件
pose_latents = self.vae.encode(pose_frames)  # 姿态帧编码

# 时间引导条件
y_reft = self.vae.encode(reference_frames[:refert_num])  # 前N帧编码
```

#### **2.3 扩散采样** (第570-636行)

```python
# 初始化噪声
x = torch.randn(batch_size, channels, frames, h, w)

# 时间步调度
timesteps = scheduler.sample_timesteps(num_steps)

# 采样循环
for t in timesteps:
    # 模型预测噪声
    noise_pred = model(x, t, context, ref_latents, ...)
    
    # 更新状态
    x = scheduler.step(noise_pred, t, x)
    
    # 可选：模型卸载到CPU（节省显存）
    if offload_model:
        model.cpu()
```

#### **2.4 解码与保存** (第637-650行)

```python
# VAE解码
video_frames = self.vae.decode(x)

# 后处理
video = postprocess(video_frames)

# 保存
save_video(video, save_path, fps=30)
```

---

## 🔑 关键代码位置

### **入口文件**
- `generate.py`: 主生成脚本，参数解析和流程控制
- `wan/animate.py`: WanAnimate类，核心生成逻辑

### **预处理相关**
- `wan/modules/animate/preprocess/preprocess_data.py`: 预处理主脚本
- `wan/modules/animate/preprocess/process_pipepline.py`: 预处理管道
- `wan/modules/animate/preprocess/pose2d.py`: 姿态检测
- `wan/modules/animate/preprocess/retarget_pose.py`: 姿态重定向

### **模型组件**
- `wan/modules/t5.py`: T5文本编码器
- `wan/modules/animate/clip.py`: CLIP图像编码器
- `wan/modules/vae2_1.py`: VAE编码解码器
- `wan/modules/animate/model_animate.py`: DiT扩散模型

### **工具函数**
- `wan/utils/fm_solvers.py`: 扩散采样器（DPM++）
- `wan/utils/fm_solvers_unipc.py`: UniPC采样器
- `wan/utils/utils.py`: 视频保存等工具函数

---

## 🎯 关键参数说明

### **预处理参数**
- `resolution_area`: 目标分辨率 [width, height]
- `replace_flag`: 替换模式（角色替换）
- `retarget_flag`: 姿态重定向模式（动作迁移）
- `refert_num`: 参考帧数（1或5，推荐1）

### **生成参数**
- `sample_steps`: 采样步数（30-50，越多质量越好但越慢）
- `sample_guide_scale`: 引导系数（5.0-7.0，控制生成多样性）
- `sample_solver`: 采样器（unipc或dpm++）
- `offload_model`: 是否卸载模型到CPU（节省显存）
- `t5_cpu`: T5编码器是否放在CPU（节省显存）

---

## 💡 优化建议

1. **显存优化**
   - 启用 `--offload_model=True`
   - 启用 `--t5_cpu`
   - 降低分辨率
   - 减少 `refert_num`（从5改为1）

2. **质量优化**
   - 增加 `sample_steps`（30→50）
   - 使用 `sample_solver=dpm++`
   - 使用 `refert_num=5`

3. **速度优化**
   - 使用 `sample_solver=unipc`
   - 减少 `sample_steps`（50→30）
   - 使用 `refert_num=1`

---

## 📊 数据流图

```
输入数据
  ├─ 参考图片 (refer_path)
  └─ 驱动视频 (video_path)
        ↓
  [预处理阶段]
        ↓
  预处理数据
  ├─ 视频帧序列
  ├─ 参考图片
  ├─ 姿态数据
  └─ 掩码数据（可选）
        ↓
  [生成阶段]
        ↓
  文本提示词 → T5编码器 → 文本特征
  参考图片 → CLIP编码器 → 图像特征
  视频帧 → VAE编码器 → 潜在特征
  姿态数据 → 姿态特征
        ↓
  [扩散模型]
  噪声 + 条件特征 → DiT模型 → 去噪 → 潜在空间视频
        ↓
  [VAE解码]
  潜在空间视频 → VAE解码器 → 像素空间视频
        ↓
  输出视频 (MP4)
```

---

## 🔍 调试技巧

1. **检查预处理输出**
   - 查看 `process_results/` 目录
   - 确认所有文件都已生成
   - 检查分辨率是否正确

2. **监控显存使用**
   - 使用 `nvidia-smi` 监控GPU显存
   - 如果显存不足，启用 `offload_model`

3. **查看日志**
   - 关注 "Creating Wan-Animate pipeline" 后的日志
   - 检查模型加载是否成功
   - 查看采样进度

4. **常见错误**
   - 返回码 3221225477: 显存不足，降低分辨率或启用offload
   - 预处理失败: 检查输入文件路径和格式
   - 模型加载失败: 检查模型权重目录

---

**文档版本**: 1.0  
**最后更新**: 2025-12-11


# Wan2.2-Animate-14B 代码注释示例

本文档提供关键代码段的注释示例，帮助理解模型运行流程。

---

## 1. 预处理阶段注释示例

**文件**: `wan/modules/animate/preprocess/preprocess_data.py`

```python
def main():
    """
    预处理主函数
    
    功能：
    1. 解析命令行参数
    2. 初始化预处理管道
    3. 执行预处理流程
    4. 保存预处理结果
    """
    args = _parse_args()
    
    # 创建预处理管道对象
    # ProcessPipeline 负责整个预处理流程的协调
    pipeline = ProcessPipeline(
        ckpt_path=args.ckpt_path,          # 预处理模型检查点路径
        video_path=args.video_path,        # 驱动视频路径（模板视频）
        refer_path=args.refer_path,        # 参考图片路径（要动画化的角色）
        save_path=args.save_path,          # 预处理结果保存路径
        resolution_area=args.resolution_area,  # 目标分辨率 [width, height]
        fps=args.fps,                      # 目标帧率（默认30fps）
        replace_flag=args.replace_flag,    # 是否使用替换模式（角色替换）
        retarget_flag=args.retarget_flag,  # 是否使用姿态重定向（动作迁移）
        use_flux=args.use_flux,            # 是否使用FLUX图像编辑
        iterations=args.iterations,         # 掩码膨胀迭代次数（替换模式）
        k=args.k,                          # 掩码膨胀核大小（替换模式）
        w_len=args.w_len,                  # 掩码网格宽度细分（替换模式）
        h_len=args.h_len,                  # 掩码网格高度细分（替换模式）
    )
    
    # 执行预处理流程
    pipeline.run()
    
    # 预处理完成后，会在 save_path 目录下生成：
    # - video/: 处理后的视频帧序列
    # - reference/: 参考图片
    # - pose/: 姿态数据（每帧对应一个姿态文件）
    # - mask/: 掩码数据（如果使用替换模式）
    # - config.json: 配置文件
```

---

## 2. 生成阶段主流程注释

**文件**: `generate.py`

```python
def generate(args):
    """
    视频生成主函数
    
    流程：
    1. 初始化分布式环境（如果使用多GPU）
    2. 加载模型组件（T5, CLIP, VAE, DiT）
    3. 准备输入数据
    4. 执行扩散采样
    5. 解码并保存视频
    """
    
    # ========== 阶段1: 分布式初始化 ==========
    rank = int(os.getenv("RANK", 0))           # 当前进程排名（0为主进程）
    world_size = int(os.getenv("WORLD_SIZE", 1))  # 总进程数（GPU数量）
    local_rank = int(os.getenv("LOCAL_RANK", 0))  # 本地GPU排名
    device = local_rank                        # 设备ID
    
    # 如果是多GPU模式，初始化分布式训练环境
    if world_size > 1:
        torch.cuda.set_device(local_rank)
        dist.init_process_group(
            backend="nccl",                    # NVIDIA GPU通信后端
            init_method="env://",              # 通过环境变量初始化
            rank=rank,
            world_size=world_size
        )
    
    # ========== 阶段2: 创建Pipeline ==========
    if "animate" in args.task:
        # 创建Wan-Animate pipeline
        wan_animate = wan.WanAnimate(
            config=cfg,                        # 模型配置（从configs加载）
            checkpoint_dir=args.ckpt_dir,      # 模型权重目录
            device_id=device,                  # GPU设备ID
            rank=rank,                         # 进程排名
            t5_fsdp=args.t5_fsdp,             # T5是否使用FSDP（多GPU分布式）
            dit_fsdp=args.dit_fsdp,            # DiT是否使用FSDP
            use_sp=(args.ulysses_size > 1),    # 是否使用序列并行
            t5_cpu=args.t5_cpu,                # T5是否放在CPU（节省显存）
            convert_model_dtype=args.convert_model_dtype,  # 是否转换精度
            use_relighting_lora=args.use_relighting_lora   # 是否使用重光照LoRA
        )
        
        # ========== 阶段3: 生成视频 ==========
        video = wan_animate.generate(
            src_root_path=args.src_root_path,  # 预处理数据路径
            replace_flag=args.replace_flag,    # 是否使用替换模式
            refert_num=args.refert_num,        # 参考帧数（1或5）
            clip_len=args.frame_num,           # 生成帧数
            shift=args.sample_shift,           # 采样偏移
            sample_solver=args.sample_solver,  # 采样器（unipc或dpm++）
            sampling_steps=args.sample_steps,   # 采样步数
            guide_scale=args.sample_guide_scale,  # 引导系数
            seed=args.base_seed,               # 随机种子
            offload_model=args.offload_model   # 是否卸载模型到CPU
        )
    
    # ========== 阶段4: 保存视频 ==========
    if rank == 0:  # 只在主进程保存
        save_video(
            tensor=video[None],                # 视频张量 [1, frames, H, W, C]
            save_file=args.save_file,          # 保存路径
            fps=cfg.sample_fps,                # 帧率（默认30）
            normalize=True,                     # 是否归一化
            value_range=(-1, 1)                 # 张量值范围
        )
```

---

## 3. WanAnimate.generate() 方法详细注释

**文件**: `wan/animate.py`

```python
def generate(self, src_root_path, replace_flag, refert_num, ...):
    """
    生成视频的核心方法
    
    参数说明：
    - src_root_path: 预处理数据路径
    - replace_flag: 是否使用替换模式
    - refert_num: 参考帧数（1或5）
    - clip_len: 每个clip的帧数
    - sampling_steps: 采样步数
    - guide_scale: 引导系数
    """
    
    # ========== 步骤1: 加载预处理数据 ==========
    # 从预处理目录加载所有必要的数据
    cond_images = load_conditioning_images()   # 条件图像（姿态帧）
    refer_images = load_reference_images()    # 参考图片
    face_images = load_face_images()          # 人脸图像（用于人脸编码）
    bg_images = load_bg_images()              # 背景图像（替换模式）
    mask_images = load_mask_images()          # 掩码图像（替换模式）
    
    # ========== 步骤2: 文本编码 ==========
    # 使用T5编码器将文本提示词编码为特征向量
    context = self.text_encoder([prompt])      # 正向提示词编码
    context_null = self.text_encoder([negative_prompt])  # 负向提示词编码
    
    # context 形状: [batch, seq_len, hidden_dim]
    # 用于指导生成过程，控制生成内容与文本的匹配度
    
    # ========== 步骤3: 图像编码 ==========
    # 使用CLIP编码器编码参考图片
    img = ref_pixel_values[0, :, 0]            # 提取参考图片
    clip_context = self.clip.visual([img])     # CLIP视觉编码
    # clip_context 用于提供视觉语义信息
    
    # ========== 步骤4: VAE编码 ==========
    # 将图像从像素空间编码到潜在空间（压缩表示）
    
    # 4.1 编码参考图片
    ref_latents = self.vae.encode(ref_pixel_values)
    # ref_latents: [batch, channels, 1, H/8, W/8]
    # 参考图片的潜在表示，用于提供角色外观信息
    
    # 4.2 编码姿态帧（条件图像）
    pose_latents = self.vae.encode(conditioning_pixel_values)
    # pose_latents: [batch, channels, frames, H/8, W/8]
    # 姿态帧的潜在表示，用于提供动作信息
    
    # 4.3 编码时间引导帧（如果refert_num > 0）
    if refert_num > 0:
        y_reft = self.vae.encode(reference_frames[:refert_num])
        # y_reft: 前N帧的潜在表示，用于时间一致性引导
        # refert_num=1: 只使用第一帧
        # refert_num=5: 使用前5帧（更好的时间一致性，但计算量更大）
    
    # ========== 步骤5: 初始化噪声 ==========
    # 在潜在空间中生成随机噪声作为起始点
    lat_h = H // 8                            # 潜在空间高度（VAE下采样8倍）
    lat_w = W // 8                            # 潜在空间宽度
    lat_t = T // 4 + 1                        # 潜在空间时间维度（下采样4倍）
    
    noise = [torch.randn(
        16,                                   # 通道数
        lat_t + 1,                            # 时间维度
        lat_h,                                # 高度
        lat_w,                                # 宽度
        dtype=torch.float32,
        device=self.device
    )]
    # 这个噪声将逐步去噪，最终生成视频的潜在表示
    
    # ========== 步骤6: 初始化采样器 ==========
    if sample_solver == 'unipc':
        # UniPC采样器：快速采样，推荐用于动画任务
        sample_scheduler = FlowUniPCMultistepScheduler(
            num_train_timesteps=1000,         # 训练时间步数
            shift=1,                          # 偏移参数
            use_dynamic_shifting=False
        )
        sample_scheduler.set_timesteps(sampling_steps)
        timesteps = sample_scheduler.timesteps
        # timesteps: 从大到小的时间步序列，如 [999, 800, 600, ..., 0]
        
    elif sample_solver == 'dpm++':
        # DPM++采样器：高质量采样，速度较慢
        sample_scheduler = FlowDPMSolverMultistepScheduler(...)
        # 类似UniPC，但使用不同的采样策略
    
    # ========== 步骤7: 扩散采样循环 ==========
    latents = noise                           # 初始化潜在表示
    
    for i, t in enumerate(tqdm(timesteps)):
        # t: 当前时间步（从大到小，如 999 -> 0）
        # 时间步越大，噪声越多；时间步越小，图像越清晰
        
        # 7.1 准备条件输入
        arg_c = {
            "context": context,                # 文本特征
            "clip_fea": clip_context,          # CLIP视觉特征
            "y": [y],                         # 参考帧和时间引导帧的潜在表示
            "pose_latents": pose_latents,      # 姿态帧的潜在表示
            "face_pixel_values": face_pixel_values,  # 人脸图像
        }
        
        # 7.2 模型前向传播（预测噪声）
        noise_pred_cond = self.noise_model(
            TensorList(latent_model_input),   # 当前潜在表示
            t=timestep,                       # 当前时间步
            **arg_c                           # 所有条件输入
        )
        # noise_pred_cond: 模型预测的噪声
        # 模型学习预测：给定当前状态和时间步，应该有多少噪声
        
        # 7.3 分类器自由引导（Classifier-Free Guidance）
        if guide_scale > 1:
            # 同时计算有条件和无条件的预测
            noise_pred_uncond = self.noise_model(
                TensorList(latent_model_input),
                t=timestep,
                **arg_null  # 使用负向提示词
            )
            
            # 引导公式：引导预测 = 无条件预测 + scale * (有条件预测 - 无条件预测)
            noise_pred = noise_pred_uncond + guide_scale * (
                noise_pred_cond - noise_pred_uncond
            )
            # guide_scale越大，生成结果越接近文本提示词，但可能过度拟合
        else:
            noise_pred = noise_pred_cond
        
        # 7.4 采样器更新（去噪一步）
        temp_x0 = sample_scheduler.step(
            noise_pred[0],                    # 预测的噪声
            t,                                # 当前时间步
            latents[0],                       # 当前潜在表示
            return_dict=False
        )[0]
        latents[0] = temp_x0                  # 更新潜在表示
        
        # 7.5 可选：模型卸载到CPU（节省显存）
        if offload_model:
            self.noise_model.cpu()
    
    # ========== 步骤8: VAE解码 ==========
    # 将潜在表示解码回像素空间
    x0 = [x.to(dtype=torch.float32) for x in x0]  # 转换精度
    out_frames = torch.stack(
        self.vae.decode([x0[0][:, 1:]])      # 解码（跳过第一帧，因为它是条件帧）
    )
    # out_frames: [batch, frames, H, W, C]
    # 从潜在空间 [lat_t, lat_h, lat_w] 解码到像素空间 [frames, H, W, C]
    
    # ========== 步骤9: 后处理 ==========
    # 如果使用了时间引导帧，需要移除重复的帧
    if start != 0:
        out_frames = out_frames[:, :, refert_num:]
    
    # 合并所有clip的结果
    videos = torch.cat(all_out_frames, dim=2)[:, :, :real_frame_len]
    
    return videos[0]  # 返回最终视频 [frames, H, W, C]
```

---

## 4. 关键数据结构说明

### 4.1 潜在空间维度

```python
# 像素空间 -> 潜在空间
H, W = 720, 1280          # 输入分辨率
lat_h = H // 8 = 90       # VAE下采样8倍
lat_w = W // 8 = 160

T = 77                    # 帧数
lat_t = T // 4 + 1 = 20   # 时间维度下采样4倍

# 潜在空间形状: [channels=16, time=20, height=90, width=160]
# 相比像素空间，潜在空间更小，计算效率更高
```

### 4.2 条件输入结构

```python
# y_ref: 参考帧条件
y_ref = concat([
    mask_ref,              # 掩码（标识参考帧位置）
    ref_latents           # 参考图片的潜在表示
])
# 形状: [channels, 1, lat_h, lat_w]

# y_reft: 时间引导条件
y_reft = concat([
    msk_reft,             # 时间掩码（标识引导帧位置）
    y_reft_latents        # 时间引导帧的潜在表示
])
# 形状: [channels, refert_num, lat_h, lat_w]

# y: 完整条件
y = concat([y_ref, y_reft], dim=1)
# 形状: [channels, 1+refert_num, lat_h, lat_w]
```

### 4.3 模型输入输出

```python
# DiT模型输入
inputs = {
    "latent": latents,           # 当前潜在表示 [16, lat_t, lat_h, lat_w]
    "timestep": t,               # 时间步 [1]
    "context": context,           # 文本特征 [batch, seq_len, hidden_dim]
    "clip_fea": clip_context,     # CLIP特征 [batch, hidden_dim]
    "y": [y],                    # 参考和时间引导条件
    "pose_latents": pose_latents, # 姿态潜在表示
    "face_pixel_values": face_images  # 人脸图像
}

# DiT模型输出
output = noise_pred              # 预测的噪声 [16, lat_t, lat_h, lat_w]
```

---

## 5. 关键参数影响说明

### 5.1 refert_num（参考帧数）

```python
# refert_num = 1
# - 只使用第一帧作为时间引导
# - 计算量小，速度快
# - 时间一致性稍差
# - 推荐用于快速生成

# refert_num = 5
# - 使用前5帧作为时间引导
# - 计算量大，速度慢
# - 时间一致性更好
# - 推荐用于高质量生成
```

### 5.2 guide_scale（引导系数）

```python
# guide_scale = 1.0
# - 不使用分类器自由引导
# - 生成结果更随机，多样性高
# - 但可能偏离文本提示词

# guide_scale = 5.0-7.0（推荐）
# - 平衡文本匹配度和生成多样性
# - 生成结果更符合提示词
# - 但可能过度拟合

# guide_scale > 10.0
# - 强烈引导，严格遵循提示词
# - 但可能生成质量下降
```

### 5.3 sampling_steps（采样步数）

```python
# sampling_steps = 20
# - 快速采样，质量一般
# - 适合快速预览

# sampling_steps = 30（标准）
# - 平衡质量和速度
# - 推荐用于大多数场景

# sampling_steps = 50（高质量）
# - 慢速采样，质量更好
# - 适合最终输出
```

---

## 6. 替换模式 vs 动作迁移模式

### 6.1 动作迁移模式（retarget_flag=True, replace_flag=False）

```python
# 功能：将参考图片中的人物按照驱动视频的动作进行动画化
# 流程：
# 1. 提取驱动视频的姿态序列
# 2. 将姿态应用到参考图片中的人物
# 3. 生成动画视频

# 关键代码：
y_reft = self.vae.encode(reference_frames[:refert_num])
# 使用参考帧作为时间引导，保持角色外观一致
```

### 6.2 替换模式（replace_flag=True）

```python
# 功能：将驱动视频中的人物替换为参考图片中的人物
# 流程：
# 1. 提取驱动视频的姿态和背景
# 2. 生成替换掩码（标识人物区域）
# 3. 将参考图片中的人物替换到驱动视频中
# 4. 使用重光照LoRA调整光照

# 关键代码：
bg_pixel_values = batch["bg_pixel_values"]    # 背景图像
mask_pixel_values = batch["mask_pixel_values"]  # 替换掩码
y_reft = self.vae.encode([
    concat([refer_t_pixel_values, bg_pixel_values], dim=1)
])
# 结合参考帧和背景，使用掩码控制替换区域
```

---

## 7. 显存优化策略

```python
# 策略1: 模型卸载
offload_model=True
# 每次前向传播后将模型卸载到CPU
# 节省显存，但速度较慢

# 策略2: T5放在CPU
t5_cpu=True
# T5编码器放在CPU上运行
# 节省显存，对速度影响较小

# 策略3: 精度转换
convert_model_dtype=True
# 将模型参数从fp32转换为bfloat16
# 显存减半，质量基本不变

# 策略4: 降低分辨率
size="512*512"  # 而不是 "1280*720"
# 显存需求与分辨率平方成正比
```

---

**文档版本**: 1.0  
**最后更新**: 2025-12-11

